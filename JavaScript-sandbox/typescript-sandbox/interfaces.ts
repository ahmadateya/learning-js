

/*
function showTodo(todo: {title:string, text:string}) {
    console.log(todo.title+': '+todo.text)
}
let myTodo = {title:'trash', text:'get rid of the trash'};

showTodo(myTodo);
*/
interface Todo {
    title:string;
    text:string
}
